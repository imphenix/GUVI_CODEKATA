#include <stdio.h>
int main() {
 int n;
 if (scanf("%d", &n) != 1) return 0;
 for (int i = 0; i < n
 int a;
 float b;
 char op;
 double c;
 double result = 0.0;
 scanf("%d %f %c %lf", &a, &b, &op, &c);
 switch (op) {
 case '+':
result = a + b + c;
break;
 case '-':
result = (a - b) + c;
break;
 case '*':
result = (a * b) + c;
break;
 case '/':
if (b != 0)
result = (a / b) + c;
else
result = a + b + c;
break;
 default:
printf("Enter correct value");
 }
 printf("%.2lf\n", result);
 }
 return 0;
}
