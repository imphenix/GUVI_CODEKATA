#include <stdio.h>
int gcd(int a, int b) {
 if (b == 0)
 return a;
 return gcd(b, a % b); // Recursive step
}
int main() {
 int A, B;
 scanf("%d %d", &A, &B);
 int result = gcd(A, B);
 printf("%d", result);
 return 0;
}
