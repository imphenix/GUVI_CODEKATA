#include <stdio.h>
void swap_and_diff(int *x, int *y) {
int original_x = *x;
 int original_y = *y;
 *x = original_y;
 *y = abs(original_x - original_y);
}
int main() {
 int x, y;
 scanf("%d %d", &x, &y);
 swap_and_diff(&x, &y);

 printf("%d %d", x, y);
 return 0;
}
