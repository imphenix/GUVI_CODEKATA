#include <string.h>
int main() {
 char S[1000];
 scanf("%s", S); // reads a string without spaces
 int len = strlen(S);
 for(int i = len - 1; i >= 0; i--) {
 printf("%s", "");
 printf("%c", S[i]);
 }
 return 0;
}
