#include <string.h>
int main() {
 char S[1000];
 scanf("%s", S);
 int count = 0;
 int len = strlen(S);
 for (int i = 0; i < len; i++) {
 char c = S[i];
 if (c=='a' || c=='e' || c=='i' || c=='o' || c=='u' ||
 c=='A' || c=='E' || c=='I' || c=='O' || c=='U') {
 count++;
 }
 }
 printf("%d", count);
 return 0;
}
